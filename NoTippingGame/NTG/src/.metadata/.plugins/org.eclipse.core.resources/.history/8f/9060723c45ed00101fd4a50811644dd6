import static java.lang.Math.*;



public class test {
	
	static int[] positions = new int[31]; 
	static int[] weights = new int[10]; 
	static int[][] totalTorque = new int [10][31]; 
	static int[][]bestFits = new int[10][2];

	
	public static void main(String[] args) 
	{
		//Compute the positions: 
		for (int i=-15, j=0; i<16; i++, j++)
		{
			positions[j] = i; 	
		}
		//Compute the weights: 
		for (int i=0; i<weights.length; i++)
		{
			weights[i] = i; 
		}
		
		//Compute the totalTorque: 
		for (int i=0, ; i<positions.length; i++)
		{		
		
		
		Integer[]torque = {-12, 30}; 
		int[] weights = {1, 4, 5, 6, 10}; 
		addingPhase(); 

	}
	
	public static String addingPhase()
	{		

		return ""; 
	}

}
